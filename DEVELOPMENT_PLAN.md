# AI 需求整理 Agent 开发计划

## 1. 计划目标

在 8 周内交付一个可供真实用户试用的只读 MVP：用户可以连接 Jira Cloud 和 Google Drive/Docs，选择资料范围，等待系统完成增量索引，并通过对话进行检索、摘要、分类及表格整理；所有关键结论都带有用户可访问的原文引用。

计划优先验证三件事：

1. 大量真实需求能否被完整、稳定地同步和检索；
2. 带引用的回答是否足够准确，能否节省产品经理的整理时间；
3. 权限隔离、模型成本和处理时延能否满足上线要求。

## 2. 基线假设与决策门

以下内容是排期假设，而不是已经确认的事实：

- 首期接入 Jira Cloud、Google Drive 和 Google Docs，暂不支持 Jira Data Center、Sheets、Slides 与 PDF。
- 首期只读，不回写 Jira 或 Google Docs；导出支持 Markdown 和 CSV。
- 单租户不超过 10 万条 Issue/文档、日变更不超过 5,000 条，试点并发用户不超过 50 人。
- 来源 ACL 必须在检索时生效；模型服务允许处理客户数据，且满足客户的数据区域与保留要求。
- 以 PostgreSQL、`pgvector`、Redis 队列和兼容 S3 的对象存储作为基线基础设施。

**第 0 周结束前必须完成决策门评审。** 若 Jira 部署类型、数据规模、权限继承方式或数据合规要求与假设冲突，应先重新估算，不应直接进入连接器开发。

## 3. 团队配置与职责

建议的最小团队如下；人数不足时可以合并角色，但职责不能省略。

| 角色 | 建议投入 | 主要职责 |
| --- | ---: | --- |
| 产品负责人 | 0.5 人 | 场景优先级、样本数据、验收与试点反馈 |
| 后端/平台工程师 | 2 人 | API、连接器、数据模型、队列、权限和审计 |
| AI/检索工程师 | 1 人 | 分块、检索、重排、提示词、评测与成本优化 |
| 前端工程师 | 1 人 | 授权流程、同步状态、对话、引用与导出体验 |
| QA/安全 | 0.5 人 | 自动化、权限测试、安全检查与发布门禁 |
| DevOps（可兼职） | 0.25 人 | 环境、密钥、监控、备份与部署 |

产品负责人负责最终验收，技术负责人负责架构决策和上线审批；涉及安全或数据处理范围变化时，必须由安全负责人复核。

## 4. 里程碑总览

| 周期 | 里程碑 | 可演示产物 | 退出条件 |
| --- | --- | --- | --- |
| 第 0 周 | 范围与数据验证 | 场景清单、样本集、威胁模型、ADR | 基线假设已确认，试点数据和账号可用 |
| 第 1–2 周 | 端到端技术切片 | 手动导入后可完成带引用问答 | 30 个评测问题可重复运行，引用可打开 |
| 第 3–4 周 | 数据接入 Alpha | Jira/Google OAuth、全量和增量同步 | 重跑无重复，失败可重试，范围可配置 |
| 第 5–6 周 | 对话整理 Beta | 筛选、会话、异步整理、Markdown/CSV | 主要场景验收通过，任务可恢复 |
| 第 7 周 | 安全与质量加固 | ACL、审计、删除、监控和成本看板 | 权限负向测试全部通过，无高危漏洞 |
| 第 8 周 | 试点发布 | 预生产演练、用户手册、回滚方案 | 发布清单签字，SLO 和支持责任明确 |

## 5. 分周实施任务

### 第 0 周：发现与技术准备

- 与 3–5 名目标用户访谈，固定最优先的 3 个输出模板及可量化的节省时间目标。
- 获取脱敏的 Jira/Google Docs 样本，统计数量、大小、字段、语言、更新频率与权限形态。
- 用 30–50 个真实问题建立评测集，每题记录期望答案、必需证据和允许的过滤条件。
- 确认 OAuth 应用、测试租户、模型供应商、数据区域、保留期与删除要求。
- 输出架构决策记录（ADR）、数据流图、威胁模型和数据分类。
- 初始化代码结构、CI、开发/测试环境、迁移框架、密钥管理和基础可观测性。

**完成定义**：未决问题有负责人和截止日期；样本不得只包含“理想文档”；团队能在 CI 中部署空服务并执行数据库迁移。

### 第 1–2 周：端到端技术切片

- 建立 `workspace`、`source_item`、`chunk`、`conversation` 等核心表和租户字段。
- 实现本地样本导入、内容哈希、版本记录、标题/段落/Issue 语义分块及嵌入。
- 实现关键词与向量混合检索、元数据过滤、候选去重和初版重排。
- 实现最小对话 API 和页面，答案返回引用 ID、片段、标题、来源版本与链接。
- 增加引用校验：模型不得引用检索结果之外的 ID；无证据时明确拒答。
- 建立离线评测命令，记录 Recall@K、引用正确率、答案忠实度、耗时和 token 成本。

**完成定义**：一个真实问题可以从浏览器出发，经检索和模型生成得到可追溯答案；评测结果有版本记录，可作为后续回归基线。

### 第 3–4 周：连接器与同步 Alpha

- 实现 Jira Cloud OAuth、项目选择、Issue/字段/评论/关联数据分页拉取。
- 实现 Google OAuth、Drive 文件范围选择、Docs 正文与表格读取。
- 实现全量同步、增量游标、API 限流退避、幂等写入、软删除和内容未变不重嵌入。
- 建立同步任务状态机：排队、运行、部分失败、成功、取消；记录数量和错误原因。
- 在 UI 中提供连接、范围选择、同步进度、最近同步时间、失败详情和重试入口。
- 为 API 分页、限流、令牌失效、重复事件、删除和超长内容编写集成测试。

**完成定义**：同一范围连续同步两次不会产生重复记录；修改、删除和撤权能在约定窗口内反映到索引；单项失败不会使整个批次不可恢复。

### 第 5–6 周：对话整理 Beta

- 支持来源、项目、日期、状态、优先级、负责人和标签过滤，并在界面显示当前查询范围。
- 保存会话和每轮检索快照，支持继续追问以及来源预览。
- 实现摘要、主题分类和表格整理三种结构化任务；输出 schema 在服务端校验。
- 对大范围请求实现异步 Map-Reduce、检查点、进度、取消、局部重试和覆盖率统计。
- 使用数据库聚合精确计数，禁止模型估算需求数量。
- 提供 Markdown/CSV 导出，并在文件中保留来源 ID 和链接。
- 扩充到至少 100 个评测问题，加入中文长文、冲突信息、无答案和提示注入样本。

**完成定义**：三个优先场景均由产品负责人按固定样本验收；中途终止任务可从检查点恢复；导出结果与界面内容一致。

### 第 7 周：权限、安全与运行质量

- 在数据库、缓存、向量检索、对象存储和任务队列中统一应用 `tenant_id`。
- 建立用户到来源 ACL 的映射，并在检索前服务端过滤；覆盖跨租户、撤权和链接猜测测试。
- 加密 OAuth 凭证，实施令牌轮换、日志脱敏、最小权限 scope 和断开连接后的数据删除流程。
- 将来源文本标记为不可信内容，测试文档内提示注入、工具诱导和数据外泄攻击。
- 建立同步成功率、队列积压、检索/生成延迟、错误率、token/费用和引用覆盖率看板与告警。
- 完成负载、故障恢复、备份恢复和依赖漏洞扫描；修复所有高危问题。

**完成定义**：权限负向测试全部通过；审计日志可以回答“谁在何时访问或导出了什么”；达到第 7 节发布门槛。

### 第 8 周：试点发布与反馈

- 在预生产环境使用接近真实规模的数据完成全量同步、增量同步、故障恢复和回滚演练。
- 准备用户指南、管理员指南、已知限制、隐私说明、支持渠道与事故响应手册。
- 灰度开放给 5–10 名试点用户，逐日查看失败查询、反馈、成本和延迟。
- 记录试点基线：任务完成时间、人工节省时间、答案采纳率和引用点击率。
- 召开 Go/No-Go 评审；未达到硬性门槛时延长试点，不带缺陷扩大范围。

**完成定义**：发布负责人、值班人和回滚决策人明确；试点数据可以支持继续投入、调整方向或停止项目的决策。

## 6. 优先级 Backlog

### P0：MVP 发布阻塞项

| Epic | 工作项 | 依赖 |
| --- | --- | --- |
| E1 基础平台 | 租户数据模型、迁移、CI/CD、配置与密钥 | 无 |
| E2 Jira 接入 | OAuth、范围选择、全量/增量同步、删除 | E1 |
| E3 Google 接入 | OAuth、Drive 选择、Docs 解析与增量同步 | E1 |
| E4 索引检索 | 分块、嵌入、全文/向量检索、重排、过滤 | E1，部分依赖 E2/E3 样本 |
| E5 对话生成 | 会话、引用、拒答、结构化输出 | E4 |
| E6 批量任务 | 队列、Map-Reduce、检查点、进度与导出 | E4/E5 |
| E7 权限安全 | ACL、隔离、审计、删除、注入防护 | 贯穿 E1–E6 |
| E8 质量运营 | 评测、看板、告警、备份、发布与回滚 | 贯穿所有 Epic |

### P1：MVP 后优先增强

- 重复需求候选与冲突候选检测，以及人工确认工作台。
- 可保存、共享和版本化的报告模板。
- Google Sheets、Slides、PDF 与 Jira Data Center 连接器。
- 用户反馈标注、困难样本回流和检索参数自动评估。
- 经审批的 Jira/Google Docs 回写草稿。

### P2：有明确需求后再建设

- 独立向量数据库或搜索集群、跨企业知识图谱、自动排期和多 Agent 协作。
- 未经人工确认的自动写操作。

## 7. 测试策略与发布门槛

### 自动化测试层级

- **单元测试**：字段映射、分块边界、内容哈希、ACL 判定、引用校验、聚合计算。
- **契约测试**：用固定响应验证 Jira/Google API 版本变化、分页和限流行为。
- **集成测试**：数据库、向量检索、队列、对象存储及模型适配层。
- **端到端测试**：授权、同步、问答、引用打开、异步整理、导出和删除。
- **离线 AI 评测**：每次检索、提示词或模型变更都运行固定数据集，并保留结果差异。
- **安全测试**：跨租户、撤权、越权 ID、提示注入、敏感日志和依赖漏洞。
- **性能测试**：基线最大数据量下的全量同步、增量延迟、并发查询和长任务恢复。

### 硬性发布门槛

1. P0 缺陷为 0，且不存在未接受的高危/严重安全问题。
2. 抽样同步字段与来源链接正确率不低于 99%，增量同步不重复计数。
3. 评测集的检索 Recall@10 不低于 90%，引用有效率为 100%。
4. 人工评审的关键结论证据支持率不低于 95%；无证据问题不得编造答案。
5. 跨租户、撤权用户和未授权来源的负向测试全部通过。
6. 95% 的普通查询在 10 秒内开始返回结果；大任务能显示进度并从检查点恢复。
7. 完成备份恢复和回滚演练，且监控能够定位同步、检索和模型调用故障。

数值门槛应在第 0 周结合真实规模校准；任何调整都要写入 ADR，不能在发布前临时降低标准。

## 8. 风险、信号与应对

| 风险 | 预警信号 | 应对措施 |
| --- | --- | --- |
| 来源权限模型复杂 | 同一文件对不同用户可见性不同 | 第 0 周确认继承策略；ACL 在检索前过滤；建立撤权测试 |
| API 限流或内容格式异常 | 同步耗时飙升、部分文档持续失败 | 游标、退避、死信队列、局部重试和解析失败样本库 |
| 检索召回不足 | 用户答案在资料中但 Top-K 无证据 | 混合检索、元数据过滤、重排和真实问题评测驱动调参 |
| 模型幻觉 | 引用与结论不一致或出现虚构 ID | 结构化引用、ID 白名单、忠实度评测和无证据拒答 |
| 大任务成本失控 | 单任务 token 或重嵌入量异常 | 内容哈希、预算上限、分批缓存、取消能力和费用告警 |
| 数据规模超出基线 | 索引/队列延迟持续超过 SLO | 压测后分区；只有证据表明需要时才迁移独立搜索组件 |
| 需求不断扩张 | P1/P2 进入当前迭代 | 产品负责人维护 P0 边界；新增范围必须交换同等工作量 |

## 9. 项目运行机制

- 每周一确认迭代目标、依赖与容量；每日 15 分钟同步阻塞项；每周五演示可运行的端到端增量。
- 每个工作项必须包含验收标准、测试方式、监控需求、权限影响与回滚方式。
- 架构、模型、权限或数据保留策略的变化必须写 ADR，并由相应负责人批准。
- 每周运行完整 AI 评测并评审退化；关键检索或模型变更在合并前运行小型回归集。
- 进度以“已通过验收的能力”衡量，不以代码量或模型调用次数衡量。

## 10. 下一步行动（未来 5 个工作日）

| 截止 | 行动 | 负责人 | 产出 |
| --- | --- | --- | --- |
| 第 1 天 | 确认试点用户、前三个场景和成功指标 | 产品负责人 | 一页范围说明 |
| 第 1 天 | 确认 Jira 类型、Google 文件类型、数据规模和 ACL | 产品 + 技术负责人 | 已签字的基线假设 |
| 第 2 天 | 开通测试 OAuth 应用、测试租户和脱敏样本 | 平台工程师 | 可用凭证与样本清单 |
| 第 3 天 | 完成数据流图、威胁模型、供应商合规检查 | 安全负责人 | 安全评审记录 |
| 第 4 天 | 建立首批 30–50 个真实评测问题 | 产品 + AI 工程师 | 版本化评测集 |
| 第 5 天 | 评审 ADR、拆分 P0 Epic、估算容量并锁定第 1–2 周目标 | 全团队 | 可执行迭代 Backlog |

如果第 5 天仍无法获得试点数据、权限策略或模型数据处理结论，应暂停实现连接器，把该问题升级为项目阻塞项；否则开发很可能在后期因合规或数据形态差异返工。
